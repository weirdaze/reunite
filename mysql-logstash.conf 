input {
    jdbc {
        jdbc_connection_string => "jdbc:mysql://localhost:3306/reunite"
        jdbc_user => "root"
        jdbc_password => "R3unite123"
        schedule => "* * * * *"
        jdbc_driver_library => "/root/mysql-connector-java-8.0.11/mysql-connector-java-8.0.11.jar"
        jdbc_driver_class => "com.mysql.cj.jdbc.Driver"
        statement => "SELECT * FROM person WHERE last_modified > :sql_last_value"
        tracking_column => "uid"
        tracking_column_type => "numeric"
        clean_run => true
    }
}
output {
    elasticsearch {
        index => "person"
        document_type => "person"
        document_id => "%{uid}"
        hosts => ["localhost:9200"]
    }
}
